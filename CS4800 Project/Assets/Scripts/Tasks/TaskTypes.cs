using UnityEngine;

public enum TaskTypes
{
    RemoveDebris = 1,
    TestCarBattery = 2,
    GetTools = 4,
    RemoveBattery = 8,
    RemoveNails = 16,
    RemovePanel = 32,
    DisposeBatteries = 64,
}
